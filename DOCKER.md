# create-vue 3.22.0 — WSC2026 starter app

This repo is a Vue starter project **generated by create-vue 3.22.0** (the WSC2026-pinned
scaffolder), served with Vite.

```bash
docker compose up --build
```

Open **http://localhost:5173**. Pinned: Node 24.1.0 / npm 11.5.0, scaffolder create-vue 3.22.0.
